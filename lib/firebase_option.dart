import 'package:flutter/material.dart';

class FirebaseOption extends StatelessWidget {
  const FirebaseOption({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}